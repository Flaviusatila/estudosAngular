import { HighlightMauseDirective } from './highlight-mause.directive';

describe('HighlightMauseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMauseDirective();
    expect(directive).toBeTruthy();
  });
});
